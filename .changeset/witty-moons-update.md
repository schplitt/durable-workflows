---
"durable-isolates": patch
---

chore: update to `@iso4/sandbox` 0.5 — `maxIsolates` is gone (`maxConcurrentRuns`/`memoryBudgetMb` govern concurrency and residency now), and docs/comments are aligned with the new iso4 surface
